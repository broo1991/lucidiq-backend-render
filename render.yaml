services:
  - type: web
    name: lucidiq-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: PERPLEXITY_API_KEY
        sync: false
